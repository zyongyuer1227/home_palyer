# IPTVPlayer 播放器体验与安全优化 Spec

## 目标

把 IPTVPlayer 收敛成一个轻量、稳定、安全的原生播放器。App 不扩展复杂源管理能力，重点优化播放入口、播放控制、频道浏览、错误恢复和敏感信息保护。

## 非目标

- 不做后端管理能力。
- 不做复杂账号系统。
- 不在客户端维护直播源。
- 不引入重型 UI 框架或复杂动画系统。

## 设计原则

- 播放优先：首页、点播页、收藏页都应尽快把用户带到播放。
- 控制少而明确：播放器层只放播放、进度、静音、全屏、画质、返回等通用控件。
- 默认保护隐私：首页和普通错误提示不展示服务器 IP、端口、播放 URL、NAS 路径。
- 失败可恢复：播放失败时给用户明确下一步，而不是只显示 toast。
- 符合行业习惯：横屏、静音、进度条、返回键逻辑按主流视频 App 预期处理。

## 当前已完成基线

- 首页不再明文展示服务器 URL，未命名服务器显示为“服务器 1 / 当前服务器”。
- App 内字体缩放限制，避免系统大字体撑爆界面。
- 播放器默认竖屏播放，由用户主动进入横屏全屏。
- 横屏全屏支持左侧滑动调亮度、右侧滑动调音量。
- 播放器使用全屏/退出全屏图标。
- 播放器底部使用全宽横向进度条。
- 退出播放器会停止播放，避免返回主界面后后台继续出声。
- 播放器已有静音/取消静音喇叭按钮。
- 频道列表已初步收紧视觉密度。

## 信息架构

### 首页

- 顶部：当前栏目标题、服务器状态入口、设置入口。
- 搜索：频道搜索。
- 筛选：全部、收藏、分组、源。
- 内容：默认紧凑频道列表，支持切换网格。
- 快捷区：后续增加最近播放。

### 点播

- 源选择、目录浏览、文件播放保持现有结构。
- 画质选择只在播放前或播放器内出现，不在列表中过度打扰。

### 我的

- 收藏频道。
- 分组。
- 后续可增加最近播放记录。

### 播放器

- 默认竖屏播放，不自动横屏。
- 顶部控制层：返回、标题、收藏、全屏/退出全屏。
- 底部控制层：
  - 点播：当前时间、总时长、全宽进度条、播放/暂停、静音、画质。
  - 直播：直播状态轨道、播放/暂停、静音、上一台/下一台、线路/重试。
- 横屏全屏：
  - 左侧上下滑动调亮度。
  - 右侧上下滑动调音量。
  - 返回键先退出全屏，再退出播放器。

## UI 规范

- 列表默认使用紧凑模式，频道台标建议 36-44dp。
- 频道行高优先控制在 56-68dp。
- 频道名称最多两行，分组或来源为次级文字。
- 收藏、节目单、播放按钮不应喧宾夺主。
- 首页服务器入口只显示状态或别名，不显示 URL。
- 播放器控件使用图标优先，避免“横屏/竖屏”等文字按钮。
- 错误和空状态使用用户可理解文案，避免暴露技术细节。

## 易用性规范

- 播放失败时提供“重试”“换线路”“返回列表”。
- 直播支持上一台/下一台。
- 支持最近播放，便于回到上次内容。
- 服务器设置页提供“测试连接”“设为默认”。
- 长按频道弹出快捷操作：播放、收藏/取消收藏、节目单。
- 播放器关闭必须停止播放；只有明确做后台播放/PiP 时才允许后台继续播放。

## 安全与隐私规范

- 首页、频道页、播放器页不展示服务器完整地址、播放 URL、NAS 路径。
- 设置页默认隐藏完整服务器地址，点击“显示”后才展示。
- dev.log 记录前应脱敏：
  - IP/域名端口脱敏。
  - token、key、auth、password 等参数脱敏。
  - NAS 文件路径只保留文件名或末级路径。
- release 包必须正式签名，不长期分发 debug APK。
- HTTPS 默认严格校验；自签名证书支持只放在高级设置，并默认关闭。
- 如果后续保存 token，需加密存储或至少避免日志输出。

## 验收标准

- App 首屏不出现服务器 IP/端口。
- 系统字体调大后首页、底栏、播放器控件不重叠。
- 进入播放页默认保持竖屏。
- 点击全屏图标后进入横屏沉浸式播放。
- 横屏返回键先退出全屏。
- 竖屏返回或点击关闭后播放停止，主界面无声音残留。
- 点播进度条横向可拖动，直播不显示误导性的可拖动进度。
- 静音按钮可切换静音和恢复音量。
- 分享 dev.log 前不包含完整服务器地址、token 或完整播放 URL。
